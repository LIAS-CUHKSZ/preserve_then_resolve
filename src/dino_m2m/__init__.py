"""DINOv2/v3 many-to-many image matching tools.

Heavy vision dependencies are imported only by the commands that need them so
the schemas and configuration helpers remain usable in lightweight tooling.
"""

from __future__ import annotations

__version__ = "0.1.0"
